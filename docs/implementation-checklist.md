# Implementation Checklist (Appendix)

One-page reference mapping each recommendation to objective, owner, metric,
and validation method. Assessors can use this table to verify that each
research finding has been translated into a deployable engineering decision.

---

| #   | Recommendation                       | Objective                                                   | Owner / Component                                   | Metric of success                                                                                             | Validation method                                                                              | Status                                                        |
| --- | ------------------------------------ | ----------------------------------------------------------- | --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------- |
| 1   | Evidence-first evaluation loop       | Make architectural claims measurable and repeatable         | Backend / `scripts/benchmark.sh`                    | Benchmark script runs without manual intervention; baseline metrics file produced                             | Run `./scripts/benchmark.sh` on a clean instance; inspect `docs/evaluation/results/`           | ✅ Script produces artifacts with strict HTTP/error validation |
| 2   | Typed contracts                      | Add structured payload alongside LLM-readable string        | Backend / `TelemetryService`                        | `typed_output_payload` and `output_schema_version` stored for tool invocations that produce structured output | Insert a typed-payload tool call; query `tool_telemetry` to verify columns populated           | ✅ Schema columns added; overloaded `recordToolUse` available |
| 3   | Dispatch safety and observability    | Prevent silent tool-name collisions as the tool count grows | Backend / `ProjectAssistantProducer`                | Startup fails with a clear message on duplicate tool names; passes cleanly on clean registration              | Add a duplicate tool name in a test class; verify `IllegalStateException` is thrown at startup | ✅ `validateToolNameUniqueness` guard in place                |
| 4   | Cache freshness policy               | Define TTL and invalidation rules per content type          | Backend / `StructuredSummaryStoreService`           | Policy document published; content-hash invalidation confirmed in service layer                               | Review `docs/cache-policy.md`; trace `upsert` path in `StructuredSummaryStoreService`          | ✅ Policy documented; content-hash already implemented        |
| 5   | Reproducibility and traceability     | Enable independent replication of benchmark results         | All / `docs/evaluation/`                            | Fixed prompt set and environment spec committed; decision log maintained                                      | Check `docs/evaluation/` for benchmark scenarios, environment spec, and decision log           | ✅ Files created in `docs/evaluation/`                        |
| 6   | Calibrate scope with risk management | Separate immediate, medium-term, and exploratory work       | All                                                 | Phase A/B/C sequencing documented with entry/exit criteria                                                    | Review §11 of `docs/recommendations-for-implementation.md`                                     | ✅ Phase plan present in §11                                  |
| 7   | Practitioner recommendation summary  | Give assessors a clear line from findings to actions        | Docs                                                | This checklist exists and is current                                                                          | Review this file                                                                               | ✅ This document                                              |
| 8   | Protect telemetry persistence        | Ensure `tool_telemetry` history survives migrations         | Backend / `TelemetryService`, `ToolTelemetryRecord` | Schema strategy in production is `update`; startup guard logs row counts; `@Strong` Javadoc on entity         | Check startup logs for telemetry confirmation line; review `ToolTelemetryRecord` Javadoc       | ✅ Guard in `TelemetryService#guardTelemetryPersistence`      |
| 9   | Standardise Support-class design     | Centralise literals and policy constants                    | Backend / `ac.uk.sussex.kn253.support`              | Zero hard-coded host strings, module names, or URL literals outside Support classes                           | Grep codebase for duplicate literals; review `BackendSupport`, `ToolSupport`, `SchemaKeys`     | ✅ Three Support classes created; literals migrated           |

---

_Last updated: 2026-04-14_
